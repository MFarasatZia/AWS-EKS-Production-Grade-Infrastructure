apiVersion: v1
kind: Secret
metadata:
  name: app-demo-secret
  namespace: dev
type: Opaque
stringData:
  DATABASE_USER: demo_user
  DATABASE_PASSWORD: change-me-123
  DATABASE_NAME: demo_db
  # For public repo, keep values obviously fake
