apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-mylera.fullname" . }}
  labels:
    app: {{ include "node-mylera.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
  selector:
    app: {{ include "node-mylera.name" . }}
